# Assets

- **resume.pdf** — Add your resume here. The site links to `assets/resume.pdf` for the "Resume" and "Resume Download" buttons.
- **images** — Optional: add project images here and update paths in `index.html` and `projects/scripted.html` (e.g. replace placeholder URLs with local paths like `assets/images/scripted-hero.jpg`).
